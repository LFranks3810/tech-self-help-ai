services:
  - type: web
    name: tech-self-help-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python backend/app.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false